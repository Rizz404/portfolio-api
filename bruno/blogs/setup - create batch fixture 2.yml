info:
  name: setup - create batch fixture 2
  type: http
  seq: 13

http:
  method: POST
  url: "{{BASE_URL}}/blogs"
  body:
    type: json
    data: |-
      {
        "translations": [
          { "locale": "en", "title": "Batch Delete Fixture Blog 2", "content": "Fixture content." }
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture blog 2 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("blogBatchId2", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Second throwaway blog for `delete - batch`. See `setup - create batch fixture 1`.
