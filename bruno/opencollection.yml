opencollection: 1.0.0

info:
  name: portfolio-api
bundled: false
request:
  auth:
    mode: bearer
    bearer:
      token: "{{accessToken}}"
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
