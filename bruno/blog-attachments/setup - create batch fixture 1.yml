info:
  name: setup - create batch fixture 1
  type: http
  seq: 9

http:
  method: POST
  url: "{{BASE_URL}}/blog-attachments"
  body:
    type: json
    data: |-
      {
        "blogId": {{attachmentParentBlogId}},
        "fileName": "batch-fixture-1.png",
        "fileUrl": "https://example.com/files/batch-fixture-1.png",
        "fileType": "image"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture attachment 1 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("blogAttachmentBatchId1", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Throwaway attachment (on the same parent blog from `setup - create parent blog`)
  created only so `delete - batch` has real ids to delete.
