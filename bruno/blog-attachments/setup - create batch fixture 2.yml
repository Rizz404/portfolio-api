info:
  name: setup - create batch fixture 2
  type: http
  seq: 10

http:
  method: POST
  url: "{{BASE_URL}}/blog-attachments"
  body:
    type: json
    data: |-
      {
        "blogId": {{attachmentParentBlogId}},
        "fileName": "batch-fixture-2.png",
        "fileUrl": "https://example.com/files/batch-fixture-2.png",
        "fileType": "image"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture attachment 2 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("blogAttachmentBatchId2", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Second throwaway attachment for `delete - batch`. See
  `setup - create batch fixture 1`.
