info:
  name: setup - create batch fixture 2
  type: http
  seq: 10

http:
  method: POST
  url: "{{BASE_URL}}/experiences"
  body:
    type: json
    data: |-
      {
        "companyName": "Batch Delete Fixture Co 2",
        "startDate": "2020-01-01",
        "translations": [{ "locale": "en", "position": "Batch Tester" }]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture experience 2 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("experienceBatchId2", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Second throwaway experience for `delete - batch`. See
  `setup - create batch fixture 1`.
