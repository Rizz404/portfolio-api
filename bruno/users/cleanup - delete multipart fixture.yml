info:
  name: cleanup - delete multipart fixture
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{BASE_URL}}/users/{{userMultipartId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("user created by create - multipart should be removed", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Also removes the profile picture this suite uploaded to Cloudinary, since
  UserService.deleteUser cleans it up as part of deleting the user.
