info:
  name: setup - create batch fixture 2
  type: http
  seq: 17

http:
  method: POST
  url: "{{BASE_URL}}/users"
  body:
    type: multipart-form
    data:
      - type: text
        name: data
        contentType: application/json
        value: |-
          {
            "nickname": "BatchFixture2",
            "email": "batch.fixture.2@example.com",
            "password": "SuperSecret123!",
            "role": "USER",
            "provider": "LOCAL",
            "translations": [{ "locale": "en" }]
          }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture user 2 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("userBatchId2", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Second throwaway user for `delete - batch`. See `setup - create batch fixture 1`.
