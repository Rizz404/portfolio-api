info:
  name: get by id - not found
  type: http
  seq: 12

http:
  method: GET
  url: "{{BASE_URL}}/users/{{testUserId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("a deleted/unknown user id is rejected with 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

        test("error message should mention the missing id", function () {
          const body = res.getBody();
          expect(body.status).to.equal("error");
          expect(body.message).to.include("not found");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  UserService throws NoSuchElementException when a user is missing, and
  GlobalExceptionHandler.handleNotFoundException now maps that to 404 (was an
  unhandled 500 before the handler was added).

  Depends on `delete by id` having run first in this run/session so `testUserId` no
  longer exists.
