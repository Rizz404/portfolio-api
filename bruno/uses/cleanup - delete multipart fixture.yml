info:
  name: cleanup - delete multipart fixture
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{BASE_URL}}/uses/{{useMultipartId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("use created by create - multipart should be removed", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Also removes the logo/picture files this suite uploaded to Cloudinary, since
  UseService.deleteUse cleans those up as part of deleting the use.
