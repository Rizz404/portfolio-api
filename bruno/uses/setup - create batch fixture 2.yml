info:
  name: setup - create batch fixture 2
  type: http
  seq: 13

http:
  method: POST
  url: "{{BASE_URL}}/uses"
  body:
    type: json
    data: |-
      {
        "itemName": "Batch Delete Fixture Tool 2",
        "category": "software",
        "translations": [{ "locale": "en" }]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture use 2 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("useBatchId2", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Second throwaway use for `delete - batch`. See `setup - create batch fixture 1`.
