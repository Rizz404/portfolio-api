info:
  name: update by id
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{BASE_URL}}/uses/{{useId}}"
  body:
    type: json
    data: |-
      {
        "itemName": "Visual Studio Code Insiders",
        "category": "software",
        "logoUrl": "https://example.com/images/vscode-insiders-logo.png",
        "pictures": [
          "https://example.com/images/vscode-insiders-1.png"
        ],
        "links": [
          "https://code.visualstudio.com/insiders"
        ],
        "translations": [
          { "locale": "en", "reasons": "Switched to the Insiders build for the latest features." }
        ]
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function () {
          expect(res.getStatus()).to.equal(200);
        });

        test("use should reflect the updated fields", function () {
          const body = res.getBody();
          expect(body.data.id).to.equal(bru.getVar("useId"));
          expect(body.data.itemName).to.equal("Visual Studio Code Insiders");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  UseController does not apply @Valid... actually it does now, but the mapper still
  overwrites every field and translations are reconciled by locale, so send the full
  body here rather than a partial patch.
