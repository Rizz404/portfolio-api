info:
  name: setup - create batch fixture 1
  type: http
  seq: 13

http:
  method: POST
  url: "{{BASE_URL}}/projects"
  body:
    type: json
    data: |-
      {
        "translations": [
          { "locale": "en", "name": "Batch Delete Fixture Project 1" }
        ],
        "status": "archived"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("fixture project 1 should be created", function () {
          expect(res.getStatus()).to.equal(201);
          const body = res.getBody();
          bru.setVar("projectBatchId1", body.data.id);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Throwaway project created only so `delete - batch` has real ids to delete. Cleaned
  up as part of that same request (batch delete, not a separate cleanup request).
