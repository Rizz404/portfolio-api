info:
  name: cleanup - delete multipart fixture
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{BASE_URL}}/projects/{{projectMultipartId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("project created by create - multipart should be removed", function () {
          expect(res.getStatus()).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Also removes the logo/image files this suite uploaded to Cloudinary, since
  ProjectService.deleteProject cleans those up as part of deleting the project.
